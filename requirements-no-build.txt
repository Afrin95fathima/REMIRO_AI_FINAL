# NO-BUILD REQUIREMENTS - Forces wheel-only installation
# Use: pip install --only-binary=all -r requirements-no-build.txt

# If ANY package tries to build from source, pip will refuse and show available wheels

streamlit
pandas
numpy  
google-generativeai
protobuf
reportlab
python-docx
requests
python-dotenv
pillow
typing-extensions
six

# Usage Instructions:
# 1. pip install --only-binary=all -r requirements-no-build.txt
# 2. If it fails, it means no pre-compiled wheel exists for your Python version
# 3. Try: conda install streamlit pandas numpy
# 4. Then: pip install google-generativeai reportlab python-docx requests