# WINDOWS-SAFE REQUIREMENTS - Avoids ALL build issues
# Use this if you get compiler/build errors on Windows

# Install these one by one if bulk install fails:
# pip install streamlit pandas numpy google-generativeai reportlab python-docx requests

# Core Framework - stable wheel version
streamlit==1.35.0

# Data Processing - guaranteed pre-compiled wheels for Windows
pandas==2.0.3
numpy==1.24.4

# AI and ML - stable versions
google-generativeai==0.7.2
protobuf==4.24.4

# Document Generation - proven Windows compatibility
reportlab==4.0.4
python-docx==0.8.11

# Utilities - rock-solid versions
requests==2.31.0
python-dotenv==1.0.0
pillow==10.0.1

# Compatibility helpers
typing-extensions==4.7.1
six==1.16.0
setuptools>=65.0.0
wheel>=0.37.0

# Dependencies that might be missing
certifi>=2023.7.22
charset-normalizer>=3.2.0
idna>=3.4
urllib3>=2.0.4