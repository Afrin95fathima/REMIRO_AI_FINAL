# BULLETPROOF WINDOWS INSTALLATION - ZERO BUILD REQUIRED
# This uses ONLY pre-compiled wheels to avoid ALL compiler issues

# Install with: pip install --only-binary=all -r requirements-windows-safe.txt

# Core Framework
streamlit==1.37.1

# Data Processing - versions with guaranteed Windows wheels
pandas==2.1.4  
numpy==1.26.0

# AI and ML
google-generativeai==0.8.0
protobuf==4.25.1

# Document Generation  
reportlab==4.2.2
python-docx==1.1.2

# Utilities
requests==2.31.0
python-dotenv==1.0.1
pillow==10.1.0

# Essential dependencies
typing-extensions==4.8.0
six==1.16.0

# Note: If this still fails, try the conda installation method in README